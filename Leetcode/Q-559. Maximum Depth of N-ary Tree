class Solution {
public:
    int maxDepth(Node* root) {
        if(root==nullptr) return 0;
        int mx=0;
        for(auto child: root->children){
            mx=max(mx,maxDepth(child));
        }
        return 1+mx;
    }
};
