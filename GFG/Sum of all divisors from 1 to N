class Solution{
    static long sumOfDivisors(int N){
        // code here
        long totalSum = 0;

        for (int i = 1; i <= N; i++) {
            totalSum += (N / i) * i;
        }

        return totalSum;
        
    }
}
